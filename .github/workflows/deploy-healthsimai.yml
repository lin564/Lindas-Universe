name: Deploy HealthSimAI to Cloudflare

on:
  push:
    branches: [main]
    paths:
      - "healthsimai/**"
      - ".github/workflows/deploy-healthsimai.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ab0fe0d5a72e29f4c9d40da04c603e2e
          workingDirectory: healthsimai
          wranglerVersion: "4.100.0"
